# Scribe-Scout Survey — planner

**Date:** 2026-05-24
**Province:** planner (`/home/user/planner`) — Holiday Planner, governance satellite
**Summoning agent:** Aurelius — Chronicler of the Order, Codex Province
**Scribe:** Scribe-Scout (canon-proc-006), Worker Tier
**Campaign:** Codex bulk-ingestion, Tier 0 PR #1 — designated smoke-test Province

> Reconnaissance survey founding the planner Province's archival profile. No existing volume
> entry in Codex's `data/volumes.json`. Planner self-identifies as a "Codex constitutional
> governance" satellite operating under Constitution of the Republic of Codex v1.1. Densest
> Codex-shaped Province by document count; recommended as the scribe-record pipeline smoke
> test for that reason.

---

## 1. Identity & ownership

- **What it is.** `planner` is a static GitHub-Pages PWA — a guided family-holiday planning dashboard called **Holiday Planner**, evolving out of a single-file monolithic dashboard (`august_family_honeymoon_dashboard_v46_5_full_qa_fix.html`, 246,106 bytes at repo root) into a split, governed PWA. Cited at `/home/user/planner/README.md:1-2` and `/home/user/planner/docs/CHARTER.md:7-8`.
- **Repo.** `Rishabh1804/planner` — `/home/user/planner/CLAUDE.md:5` and `/home/user/planner/docs/DECISIONS.md:5`.
- **Stack.** Static HTML/CSS/JS PWA. No `package.json` present. Root deployment-critical files: `index.html` (157,702 bytes), `styles.css` (79,255 bytes), `app.js` (661 bytes — *a deliberate boot guard, not the runtime*; `/home/user/planner/app.js:1-17`), `sw.js`, `manifest.json`. Live runtime is still inline in `index.html` (`/home/user/planner/docs/DECISIONS.md:70-79`, `/home/user/planner/docs/QA.md:14-17`).
- **Builder / Companion.** No companion is named in the planner repo itself. Uncertain claim: based on Codex's CLAUDE.md, planner is *not yet enrolled in any Cluster A/B/Monument*. The planner repo declares itself "an independent satellite app with shared governance" under "Constitution of the Republic of Codex v1.1" (`/home/user/planner/docs/CHARTER.md:13-14`) but does not name a Builder. Cannot assert a Builder from on-disk evidence alone — recommend Aurelius confirm or seat one at ingest time.
- **current_phase (proposed).** "Live v1 — Options runtime hybrid breakage; replacement blocked on `index.html` SHA conflict. Source modules (`src/state`, `src/data`, `src/engine`, `src/pwa`, `src/ui`) exist as non-wired scaffolds. Next gate: clean live Options replacement using `src/ui/options-view.css`." Drawn from `/home/user/planner/CLAUDE.md:9-21,37-43`, `/home/user/planner/docs/qa/2026-05-05-live-options-replacement-attempt.md:32-48`, `/home/user/planner/docs/RELEASES.md:5-12`.
- **Last meaningful commit.** `fbc57f6 docs: add planner options handoff log` at `2026-05-05 23:37:11 +0530`. Last *code* (non-docs) commit was `8fe349b fix: refresh pwa cache for options css`. The repo has been quiet since 2026-05-05; per Codex's date (2026-05-24) this is ~19 days idle.

## 2. Active work

- **In-flight chapters** (Scout reading; ambiguous because planner uses "phases" not "chapters"):
  - **Phase 0 — Stabilize current PWA.** `/home/user/planner/docs/ROADMAP.md:3-11`. Status: largely complete per `/home/user/planner/docs/RELEASES.md:7-12` (Live v1 baseline).
  - **Phase 1 — Organize repo sections.** `/home/user/planner/docs/ROADMAP.md:13-19`. Status: in progress (`assets/`, `docs/`, `archive/`, `src/` scaffolds all exist).
  - **Phase 2 — Split the bundle.** `/home/user/planner/docs/ROADMAP.md:21-39`. Status: scaffolds present but **non-wired** (`/home/user/planner/docs/QA.md:17`).
  - **Phase 3 / Phase 4 — not yet started.**
  - **Active workstream (CLAUDE.md priority):** mobile **Options** tab repair — replace hybrid/legacy runtime markup with one coherent Options runtime structure (`/home/user/planner/CLAUDE.md:7-21`).
- **TODOs / ROADMAP.**
  - No `TODO.md` or `TODOS.md` exists.
  - TODOs are scattered as inline checklists across `docs/BUGS.md:23-37` (Active regression checklist, 10 items), `docs/QA.md:33-51` (Required retest checklists), and the QA gates in `docs/qa/options-live-replacement.md:24-107` (~50 unchecked boxes).
  - Acceptance criteria for the open Options work at `/home/user/planner/docs/session-handoffs/PLANNER_SESSION_HANDOFF_2026-05-05.md:233-243` (9 criteria).
  - Roadmap proper at `/home/user/planner/docs/ROADMAP.md`.

## 3. Canon-like artifacts

Planner has no `canons/` directory and no `charter-001` ID. The prior plan's reference to "charter-001 as a planner artifact" is **not corroborated by anything on disk** — Scout names this uncertainty plainly.

What it *does* have, that maps to Codex-canon shape:

- **`docs/CHARTER.md`** (`/home/user/planner/docs/CHARTER.md`, 66 lines) — single Charter document with Status, Purpose, Authority, Principles (8 bulleted invariants, lines 28-37), Architecture, Stabilization Goals, Non-goals. This is the closest thing to a planner "charter-001" — Scout proposes ingesting it *as* `planner-charter-001` or similar.
- **`docs/DECISIONS.md`** (`/home/user/planner/docs/DECISIONS.md`, 197 lines, 11 dated decisions, all 2026-05-05) — these are pure canon-shaped entries: Decision / Rationale / Consequences triplet for each.
- **`docs/contracts/`** — 4 contracts, all canon-shaped (boundary rules, ownership, non-goals):
  - `options-contract.md` (183 lines)
  - `data-catalog-contract.md` (221 lines)
  - `destination-intelligence-contract.md` (332 lines)
  - `budget-intelligence-contract.md` (300 lines)
- **`docs/STRUCTURE.md`** (`/home/user/planner/docs/STRUCTURE.md`, 186 lines) — "Migration Rule" at lines 171-183 and "Constitutional Note" at lines 184-186 are canon-shaped invariants.
- **Embedded section-level "Rule" blocks** in each `src/*/README.md` and `src/ui/{planner,options,decide,ready}.md` — short ownership invariants (e.g., `/home/user/planner/src/ui/options.md:13-15`, `/home/user/planner/src/ui/planner.md:14-16`, `/home/user/planner/src/ui/decide.md:14-15`, `/home/user/planner/src/ui/ready.md:13-14`).

Ambiguity: planner contracts are explicitly tagged `Status: Draft` (e.g., `/home/user/planner/docs/contracts/data-catalog-contract.md:3`). Whether drafts count as canons or as proto-canons is your judgment call — flagging it.

## 4. Schism-like artifacts

Planner has no dedicated rejected-approaches file. Rejected paths *do* exist, embedded in other documents — and they read very cleanly as schisms:

- **Options runtime enhancer rejected.** `/home/user/planner/docs/DECISIONS.md:181-197` ("Extract Options cleanly, not through runtime patching") — rejects `app.js` enhancer over legacy DOM in favour of governed source modules. Strongest schism candidate.
- **Another CSS containment patch rejected.** `/home/user/planner/CLAUDE.md:13-21` — explicitly rejects CSS-containment patching as the fix path for the Options breakage.
- **Architectural "Not allowed" list.** `/home/user/planner/docs/contracts/options-contract.md:25-38` enumerates four banned implementation patterns (second hidden implementation, cockpit shell via `app.js`, duplicated recommendation state, scenario controls inside Options).
- **Boot-runtime duplication rejected.** `/home/user/planner/docs/DECISIONS.md:65-79` — inline `index.html` runtime + parallel `app.js` runtime path was the original sin; resolved by reducing `app.js` to a boot guard.
- **`localStorage` namespace rename rejected (for now).** `/home/user/planner/docs/DECISIONS.md:50-63` — preserve `augustFamilyHoneymoon.*.v431` rather than immediately migrate.
- **Root `icon.svg` rejected** in favour of `assets/icons/icon.svg`. `/home/user/planner/docs/DECISIONS.md:81-93`.

Ambiguity: planner records these as decisions, not as schisms. The schism *content* is real; the schism *form* is not. Recommend ingesting as schism entries with `canon_id` pointers back to the corresponding DECISIONS entry.

## 5. Lore-like artifacts

Lore-shaped material exists but is dispersed:

- **Origins.** `/home/user/planner/docs/DECISIONS.md:3-19` ("Create Holiday Planner as a separate governed app") — the founding decision and rationale. Also the README's "Notes" paragraph (`/home/user/planner/README.md:15-16`) on preserving `augustFamilyHoneymoon.*.v431` localStorage keys as a lineage trace.
- **Cautionary tales.** `/home/user/planner/docs/synthesis/2026-05-05-planner-options.md:236-275` ("Failure modes" — six named failure modes with prevention). Same shape repeats in every other synthesis report (consistent across 9 reports).
- **Doctrines.** `/home/user/planner/docs/synthesis/README.md:104-105` ("Core rule: A feature is not considered fully understood until its interaction with adjacent features has been synthesized."). Every synthesis report ends with a "Core rule" — e.g., `/home/user/planner/docs/synthesis/2026-05-05-planner-options.md:399-405`. These are *exactly* lore/doctrine shape.
- **Chronicles.** `/home/user/planner/docs/RELEASES.md:5-37` ("Live v1 Baseline — 2026-05-05") is a release chronicle. `/home/user/planner/docs/session-handoffs/PLANNER_SESSION_HANDOFF_2026-05-05.md` is a session chronicle.

## 6. Apocrypha-like artifacts

Lighter than other categories, but present:

- **Foretold (planned/aspirational).**
  - `/home/user/planner/docs/synthesis/INDEX.md:29-37` — six **Planned synthesis reports** that don't yet exist.
  - `/home/user/planner/docs/ROADMAP.md:41-52` Phase 3 and Phase 4 — future-tense features.
  - `/home/user/planner/docs/contracts/budget-intelligence-contract.md` and `destination-intelligence-contract.md` are explicitly *foretold databases* — Status: Draft, "future versions will include..." (`budget-intelligence-contract.md:10`).
- **Forgotten (abandoned/superseded).** None explicitly. The synthesis index uses a "Superseded" status (`/home/user/planner/docs/synthesis/INDEX.md:42`), but no report currently carries it.
- **Fulfilled.** Implicit only — Live v1 Baseline at `/home/user/planner/docs/RELEASES.md:5-37` is the closest fulfilled-foretelling artifact (it lists what the stabilization phase delivered against original goals).
- **The placeholder-artifact saga.** `/home/user/planner/docs/qa/2026-05-05-live-options-replacement-attempt.md:55-69` — a created-then-immediately-deleted placeholder (commit `9dfafe6` create, `c13d5d2` delete). This has clear apocryphal flavour — an artifact that briefly existed and was deliberately erased.

## 7. Specs

Planner has no `specs/` directory. What it has instead:

- **`docs/contracts/`** (4 files, 1,036 lines combined) functions as planner's spec equivalent — boundary/ownership specs for Options, Data Catalog, Destination Intelligence, Budget Intelligence.
- **`docs/STRUCTURE.md`** (`/home/user/planner/docs/STRUCTURE.md`, 186 lines) — architectural target spec, Current Safe Layout vs Target Layout.
- **`docs/ROADMAP.md`** (52 lines) — phased delivery spec.
- **No design-document folder, no `docs/specs/` mirror** like Codex/SproutLab use.

Recommend mapping `docs/contracts/*` and `docs/STRUCTURE.md` to Codex `specs[]` entries.

## 8. Companion-log-like artifacts

- **One formal handoff:** `/home/user/planner/docs/session-handoffs/PLANNER_SESSION_HANDOFF_2026-05-05.md` (253 lines) — session goal, GitHub state, root-cause assessment, IDs to preserve, function-level guidance, acceptance criteria, important note. Textbook companion-log shape.
- **Five QA session notes** in `/home/user/planner/docs/qa/` (2026-05-05 dates) — session-flavoured QA records.
- **Bug-review notes** in `/home/user/planner/docs/bugs/` (8 individual files + README) — focused per-bug reviews.
- **CLAUDE.md "Tooling Notes From 2026-05-05 Session"** at `/home/user/planner/CLAUDE.md:162-168` — DNS-failure note, GitHub-connector blob-only quirk. Companion-log-shaped tooling memo.
- **No `docs/companion-logs/<repo>/` canonical canon-0053 path.** This is a smoke-test gap — if Codex requires v1-compliant companion logs at canonical paths (per `/home/user/Codex/CLAUDE.md` mention of canon-0053), planner has none.

## 9. Volume metadata for planner

- **Proposed `id`.** `planner`.
- **Proposed `name`.** `Holiday Planner` (canonical product title per `/home/user/planner/docs/DECISIONS.md:20-33`).
- **Proposed `shelf`.** `active` — Live v1 deployed at `https://rishabh1804.github.io/planner/` (`/home/user/planner/docs/RELEASES.md:9`) and CLAUDE.md priority work is open (`/home/user/planner/CLAUDE.md:9-11`). However, the work is *paused mid-flight* (last commit 2026-05-05, today 2026-05-24 — 19 days idle). If your shelving convention treats >2 weeks idle as `paused`, this could go either way. Stating position: **active** with a note that recent activity is stale.
- **Proposed `current_phase`.** As drafted in §1: roughly "Live v1 — Options runtime hybrid breakage; replacement blocked on `index.html` SHA conflict. Source modules scaffolded but non-wired. Next gate: clean live Options replacement."
- **Proposed `domain_color`.** Prior plan says **teal-blue**. Confirming. Rationale:
  - Existing Codex palette (from `/home/user/Codex/data/volumes.json`): SproutLab `#E8B4B8` (pink, parenting), sep-dashboard `#7A9E7E` (green), sep-invoicing `#6B8EAD` (steel blue), codex `#C4A87A` (parchment), businessai-simulation `#A08870` (taupe), command-center `#8b7355` (brown), temple-of-mars `#C41E3A` (crimson), msc `#7C9CBF` (slate blue).
  - sep-invoicing already holds `#6B8EAD` and msc holds `#7C9CBF` — both in the steel/slate-blue family. A teal-leaning hue (e.g., `#5F9EA0` cadet teal, `#4A8B8E` deeper teal, `#6FA8A6` muted teal) would sit cleanly between travel/sea associations without colliding. Stating position: **teal-blue confirmed**, suggested concrete hex `#5F9EA0` or `#4A9B97` — pick to your taste; flag is the *family*, not the exact code.
- **Proposed `tags`.** `pwa`, `travel`, `family`, `mobile-first`, `static-pages`, `governance-satellite`. Drawn from `/home/user/planner/docs/CHARTER.md:7-9,28-37`.
- **`repo`.** `rishabh1804/planner` (`/home/user/planner/docs/DECISIONS.md:5`).
- **`description` (drafted).** "Static mobile-first PWA for guided family-holiday planning. Single-source Planner-tab intent fans out to Options (compare), Decide (react), Ready (prepare). Codex governance satellite; Constitution v1.1 governs. Hosted at rishabh1804.github.io/planner/."

## 10. Cross-volume references

- **Explicit reference to `codex` (the Volume).** Throughout — `/home/user/planner/docs/CHARTER.md:13-14`, `/home/user/planner/docs/DECISIONS.md:3-19`, `/home/user/planner/docs/STRUCTURE.md:3,184-186`. Planner self-identifies as a "Codex constitutional governance" satellite. This is the dominant cross-volume tie.
- **No explicit reference to `sproutlab`, `sep-*`, `msc`, `command-center`, `temple-of-mars`, `businessai-simulation`.**
- **Implicit similarity to `sproutlab`** — both are single-author PWAs (per the SproutLab volume description). Worth noting for cluster-assignment consideration but not asserted by the repo itself.

## 11. Data-integrity surprises

- **`charter-001` not found.** The prior plan named `charter-001` as a planner artifact. There is no file or ID by that name on disk. Closest match: `docs/CHARTER.md` (no ID prefix). **Flag for Aurelius:** confirm whether `charter-001` was an anticipated-ingest-time identity (i.e., the *Codex* canon ID it will be given), or a stale assumption.
- **`docs/BUGS.md` vs `docs/bugs/`.** `docs/BUGS.md:1-3` says "This file is the persistent bug memory." `docs/bugs/README.md:1-9` says the folder is "focused bug-review notes extracted from the broader Holiday Planner bug tracker." So BUGS.md is the master and bugs/ is per-bug detail. The split is intentional but easy to double-count at ingest. 8 BUG-XXX entries exist in both (BUG-001 through BUG-008).
- **`docs/bug.md`** (lowercase) is *not* a bug tracker — it's a single-bug review file currently holding BUG-008 narrative (`/home/user/planner/docs/bug.md:9-71`). Naming collision with `docs/BUGS.md` and `docs/bugs/` is a footgun. State plainly: this looks like a vestige that should not be ingested as a third bug source.
- **Synthesis duplication.** Both `/home/user/planner/docs/synthesis/data-engine.md` (215 lines) and `/home/user/planner/docs/synthesis/2026-05-05-data-engine.md` (482 lines) exist. Unclear if the shorter is a stub or superseded — neither states "superseded by" on disk. Likely the un-dated one is the older draft; the dated one is canonical. Confirm before ingest.
- **`docs/contracts/*` are all `Status: Draft`** (every contract). Whether drafts go into `canons` or are held as `apocrypha` (foretold) is a Codex convention call.
- **Live `index.html` content not in this clone.** Per `/home/user/planner/CLAUDE.md:162-168` and the QA-attempt note (`/home/user/planner/docs/qa/2026-05-05-live-options-replacement-attempt.md:32-48`), there is a known SHA-divergence problem between this local copy and the actual GitHub `index.html` (rejected base SHA `ef2f0ad8...`, expected current `355d739b...`). The 157,702-byte `index.html` on disk may be stale. Naming this uncertainty: ingestion shouldn't trust *runtime line-references inside index.html* as stable cross-cites.
- **No Codex canonical paths.** `docs/companion-logs/<repo>/` does not exist. If canon-0053 requires it for Province enrolment, planner needs one created (out of scribe-scout scope; flagging only).
- **No `package.json`.** Confirmed absence. Stack-inference is "vanilla static HTML/JS/CSS" — no build, no deps.
- **`holiday-planner-pwa-split-scaffold.zip`** at repo root (82,399 bytes) is a generated handoff scaffold that should arguably live under `archive/scaffolds/` per `/home/user/planner/archive/README.md:6-12`. Drift from the project's own structure rule.

## 12. Counts — approximate per entity type

Working from on-disk evidence; "approximate" because some artifacts map to multiple Codex entity types and the contract-vs-canon-vs-spec call is yours.

| Codex entity type | Planner count (approx) | Source / how counted |
|---|---|---|
| **volume metadata** | 1 | the Volume entry itself |
| **chapters[]** | ~4 | Phases 0–3 per `/home/user/planner/docs/ROADMAP.md`; Phase 4 is hygiene-only |
| **todos[]** | ~70 line-items | aggregated checkboxes: BUGS.md:23-37 (~10), QA.md:33-51 (~12), `docs/qa/options-live-replacement.md:24-107` (~50). Conservative compression to ~15-25 *distinct* TODO entries is likely more useful |
| **canons[]** | ~30 | 1 charter + 11 DECISIONS entries (`docs/DECISIONS.md`) + 4 contracts treated as canons (or held as apocrypha — see §11) + 8 short Rule blocks inside `src/*/README.md` and `src/ui/*.md` + a Migration Rule + Constitutional Note from STRUCTURE.md. If contracts are excluded: ~22. |
| **schisms[]** | ~6 | per §4 enumeration: options-runtime-enhancer, CSS-containment, options-contract "Not allowed" cluster (counts as 1 or 4), duplicate-runtime, localStorage-rename-deferred, root-icon |
| **lore[]** | ~15 | 1 Origins (founding decision) + 9 synthesis "Core rule" doctrines + 9 synthesis "Failure modes" sets as cautionary tales (compressed: ~9 lore entries from synthesis, ~1 origin, ~4 release/handoff chronicles) |
| **apocrypha[]** | ~10 | 6 planned synthesis reports + 2 future-database contracts (budget + destination, as foretold) + Phase 3/4 roadmap entries + 1 placeholder-artifact-saga |
| **specs[]** | ~5-6 | 4 contracts + STRUCTURE.md + ROADMAP.md (if treated as a delivery spec) |
| **companion-logs[]** | ~7 | 1 formal session handoff + 5 QA session notes + 1 CLAUDE.md tooling-notes block. No canon-0053-compliant logs exist. |
| **doctrine-ledger entries** | ~3-4 | "Core rule" entries that read as cross-feature doctrine (the synthesis-system "feature-not-understood-until-synthesized" rule, the Planner-source-of-truth rule, the data/engine/options/ready boundary rule, the deploy-asset/cache discipline implied across BUGS.md) |
| **bugs (BUG-XXX)** | 8 | BUG-001…BUG-008 across `docs/BUGS.md` and `docs/bugs/*.md`. Codex schema has no `bugs[]` per your enumeration — these likely fold into `companion-logs` or a Province-local TODO grouping |

**Total ingest estimate: ~145-180 entries.** Concentrated in canons (~30), todos (~70), lore (~15), apocrypha (~10), specs (~6), companion-logs (~7), schisms (~6), chapters (~4), volume metadata (1). The single largest dollar of work is decomposing the synthesis reports (9 documents, ~3,200 lines combined) into lore + canon + apocrypha components rather than ingesting each as one opaque blob.

---

### Smoke-test observations for the scribe-record pipeline

Three things this Province will stress that others may not:

1. **Multiple overlapping bug surfaces** (BUGS.md, bugs/*.md, bug.md) — your ingest must pick one canonical, not triple-count.
2. **`Status: Draft` contracts** — needs an explicit policy: do drafts become canons, apocrypha, or held in a draft-staging area?
3. **No CharterID prefixing convention exists on disk** — every other planner doc relies on filename. If Codex needs IDs like `planner-canon-001`, those must be minted at ingest, not read.

Files most worth opening early in the pipeline build: `/home/user/planner/docs/DECISIONS.md` (cleanest canon-shaped source, 11 well-formed entries), `/home/user/planner/docs/synthesis/INDEX.md` (best single index of what synthesis contains), `/home/user/planner/docs/contracts/options-contract.md` (densest, most-cross-referenced contract).

---

## Open rulings for the Chronicler

1. **Builder seat** — none seated. Cluster-unenrolled. Constitutional decision: leave open, or seat now?
2. **`charter-001` identity** — anticipated-ingest-time Codex ID, or stale plan assumption? Resolve before ingest mints IDs.
3. **`Status: Draft` contracts policy** — drafts → canons, → apocrypha (foretold), or → draft-staging tier?
4. **Bug-source canonicalization** — pick one of: `docs/BUGS.md`, `docs/bugs/`, `docs/bug.md`. Recommend `docs/BUGS.md` master + `docs/bugs/` per-bug detail; treat `docs/bug.md` as vestige and exclude.
5. **`bugs[]` entity** — Codex schema has no `bugs[]` per known taxonomy. Fold BUG-XXX into companion-logs, into a Province-local TODO grouping, or extend schema?
6. **Synthesis duplication** — `synthesis/data-engine.md` (215L) vs `synthesis/2026-05-05-data-engine.md` (482L). Confirm dated is canonical; mark un-dated superseded or delete.
7. **`docs/contracts/*` taxonomy** — canons + specs (double-file), or specs only?
8. **`charter-001` ID minting** — if Codex needs `planner-canon-001` etc., the ID-namespace plan in Tier 0 PR #2 must cover Province-prefixed canons.
9. **domain_color specific hex** — teal-blue family confirmed. Suggested `#5F9EA0` or `#4A9B97`.
10. **Shelf** — `active` per live deploy, but 19 days idle. Default to `active` unless Sovereign confirms paused-by-intent.

---

*Survey conducted 2026-05-24 by Scribe-Scout under summon by Aurelius. Drafted into archival form by Aurelius from the Scout's findings brief. Permission floor of canon-proc-006 observed: Scout performed no Writes, no Edits, no commits.*
