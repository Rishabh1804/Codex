# Scribe-Scout Survey — work-hub

**Date:** 2026-05-24
**Province:** work-hub (`/home/user/work`)
**Summoning agent:** Aurelius — Chronicler of the Order, Codex Province
**Scribe:** Scribe-Scout (canon-proc-006), Worker Tier
**Campaign:** Codex bulk-ingestion, Tier 0 PR #1

> Reconnaissance survey founding the work-hub Province's archival profile. work-hub
> has no existing volume entry in Codex's `data/volumes.json`; this survey is the
> first archival mention. Citations are file:line against `/home/user/work` at
> commit `1964b20` (default branch `main`).

---

## 1. Identity & ownership

- **What it is.** A small React + Vite + Tailwind + shadcn/ui single-page app — a personal "Work Hub" landing/launcher for the user (Rishabh). The HTML import at `src/imports/work-hub.html:7` titles it "Work Hub"; the React port lives in `src/app/App.tsx:117-159`. Package metadata still reads `@figma/my-make-file` (`package.json:2`) and `index.html:7` still bears the title "Review HTML Code" — heritage from Figma Make scaffolding.
- **Builder / persona.** Sole author across all commits: `Rishabh Jain <Rishabh1804@gmail.com>` (git log, `git show 03b0234` / `50775e4` / `1964b20`). Hardcoded user is `Rishabh` (`src/app/App.tsx:48`, `src/imports/work-hub.html:89`). No Claude / companion attribution on any commit.
- **Cluster.** Personal / operations cluster — the hub is a launcher *into* SEP work (Soma Electro Products) and a placeholder for AI projects (`src/app/App.tsx:54-72`). It sits "above" SEP-domain provinces rather than alongside them.
- **Current phase.** Static prototype, footer-stamped `"Work Hub v0.1 · Bridge-only Claude integration · Static prototype · 11 May 2026"` (`src/app/App.tsx:156`, `src/imports/work-hub.html:158`). No tests, no backend, no persistence — the config is hardcoded into `defaultConfig` (`src/app/App.tsx:46-88`).
- **Last meaningful commit.** `1964b20` "Add base path for Pages + GitHub Actions deploy workflow" — 2026-05-12 15:20:30 +0530. This is a merge commit (parents `50775e4` and `03b0234`) — and it carries the README conflict (see §11).
- **Branches.** Two refs, both at `1964b20`: `main` and `claude/codex-persona-registry-update-t3i7D`. The branch name hints at a Codex-related effort but there are no commits beyond the merge — the branch exists only as a label.
- **Deploy target.** GitHub Pages at `rishabh1804.github.io/work/` (`vite.config.ts:20-22`, `.github/workflows/deploy.yml`). Base path `/work/` is hard-coded.

## 2. Active work

- **In-flight chapters.** None formally tracked. The "in flight" work, inferred, is: (a) wire Figma Make scaffold into a working personal hub (largely done — three commits); (b) the unresolved README merge (committed, not resolved); (c) deep-link wiring for Claude Code mobile & Desktop is explicitly deferred (TODO at `src/app/components/LaunchPanel.tsx:21` and `src/imports/work-hub.html:190`).
- **TODOs in code.**
  - `src/app/components/LaunchPanel.tsx:21` — "Deep-link to Claude Code mobile app — TODO: hook to claude://code/... once Anthropic publishes the URL scheme."
  - `src/imports/work-hub.html:190` — same TODO mirrored in the legacy HTML import.
  - `src/app/components/LaunchPanel.tsx:22` and `src/imports/work-hub.html:192` — "Open Claude Desktop manually for now. Custom deep-link scheme TBD." (TBD, not strictly TODO, but functionally the same.)
- **Tasks listed inside the app UI** (these are *data*, not engineering tasks against this repo — they are SEP-domain priorities the hub surfaces): five tasks at `src/app/App.tsx:74-80` (and mirrored at `src/imports/work-hub.html:140-144`), three HIGH / two MED, **all tagged `SEP`**.
- **Soma-internal ownership.** Confirmed at the *displayed-data* level only, not at a Codex `owner` metadata level. The "Codex" button on the SEP project card links to `https://github.com/Rishabh1804/soma-internal` (`src/app/App.tsx:60`; `src/imports/work-hub.html:113`). I find no other "soma-internal" string in this province. Ambiguity worth flagging: the campaign brief says work-hub "reportedly has tasks owned by soma-internal" — what this province actually shows is that the five rendered tasks are SEP-tagged and the SEP project card points outward at `Rishabh1804/soma-internal` as its "Codex". The simplest reading: the five tasks belong to the soma-internal Province and are *surfaced* here, not *owned* here. Worth confirming against the soma-internal Province survey before ingest.

## 3. Canon-like artifacts (rules, conventions, invariants)

Thin but present.

- **HR-style invariant: keep React + Tailwind plugins.** `vite.config.ts:26-28` — comment: *"The React and Tailwind plugins are both required for Make, even if Tailwind is not being actively used — do not remove them."* This is an explicit do-not-touch rule.
- **Raw-import allowlist invariant.** `vite.config.ts:38` — *"File types to support raw imports. Never add .css, .tsx, or .ts files to this."* Hard constraint on `assetsInclude`.
- **PostCSS configuration convention.** `postcss.config.mjs:3-13` — *"Tailwind CSS v4 (via @tailwindcss/vite) automatically sets up all required PostCSS plugins — you do NOT need to include `tailwindcss` or `autoprefixer` here."*
- **Deploy base-path coupling.** `vite.config.ts:20-22` — base `/work/` must match the repo name; this is an architectural invariant for GitHub Pages.
- **Design tokens / palette.** Inline-coded throughout (`src/app/App.tsx`, `src/app/components/*.tsx`, `src/imports/work-hub.html:15-37`, `src/styles/theme.css`). Two parallel design systems coexist in the repo: a Claude-warm sand/cream palette used by the actual hub UI (`--bg: #faf9f5`, `--surface-2: #f5f4ed`, `--text-head: #1f1e1d`, accent `#b45a37` in the manifest theme-color at `src/imports/work-hub.html:8`) and a generic shadcn OKLCH theme in `src/styles/theme.css` and `default_shadcn_theme.css` that the hub does not visibly use. Categorization is ambiguous: this is either a half-finished consolidation (lore-like — "two systems, one survived") or a deliberate "shadcn boilerplate for future components" decision. I cannot tell from the artifacts alone.
- **Guidelines.** `guidelines/Guidelines.md` is the Figma Make template stub — the only non-commented line is `**Add your own guidelines here**` (`guidelines/Guidelines.md:1`). No project-specific canons are written down.

## 4. Schism-like artifacts (rejected approaches)

- **The HTML monolith vs. the React port.** `src/imports/work-hub.html` is a self-contained ~205-line static HTML page (with inline CSS and inline JS) that implements the entire hub. `src/app/App.tsx` plus four components in `src/app/components/` is the React reimplementation of the same UI, almost line-for-line. The React version is what `src/main.tsx` mounts (`src/main.tsx:3`). The HTML file persists under `src/imports/` — neither deleted nor referenced. Inferred schism: the monolithic HTML approach was *codified into React* (see commit `50775e4` subject: "Initial: Figma Make codified — React + Vite + Tailwind + shadcn/ui") and the original was retained as a reference import. This is the closest thing to a rejected-approach artifact in the province.
- **Inferred only.** No explicit "we tried X and stopped" notes anywhere.

## 5. Lore-like artifacts (Origins, Cautionary Tales, Doctrines, Chronicles)

- **Origins (inferred from commits).** The province begins as `03b0234` "Initial commit" — a one-line README "# work". Then `50775e4` codifies a Figma Make export into a React / Vite / Tailwind / shadcn scaffold. Then `1964b20` merges the two histories and adds GitHub Pages plumbing — and leaves the README conflict in place. The whole story is three commits long.
- **Cautionary tale (latent).** The merge commit `1964b20` that ships an unresolved conflict marker into `main` is itself a small cautionary tale — a "ship-and-forget" merge that nobody came back to clean up.
- **Doctrines.** None written. The invariants in §3 are the closest analogue, and they are localized to single files.
- **Chronicles.** No CHANGELOG, no NOTES, no history doc. The git log is the chronicle.

## 6. Apocrypha-like artifacts (foretold / forgotten / fulfilled)

- **Foretold — Claude Code mobile deep-link.** "TODO: hook to claude://code/... once Anthropic publishes the URL scheme." (`src/app/components/LaunchPanel.tsx:21`, mirrored at `src/imports/work-hub.html:190`). Awaits an external precondition.
- **Foretold — Claude Desktop deep-link.** "Custom deep-link scheme TBD." (`src/app/components/LaunchPanel.tsx:22`, `src/imports/work-hub.html:192`).
- **Foretold — AI projects shelf.** The "AI projects" project card is a `placeholder` with `meta: ['0 repos', 'Not yet scoped']`, focus: *"Future home for AI-side work. Add projects here as they get repo-backed."* (`src/app/App.tsx:66-72`, mirrored at `src/imports/work-hub.html:121-134`). A foretold-but-empty province slot.
- **Foretold — BM role page.** SEP project's actions include `{ label: 'BM role', url: 'bm-role.html', type: 'internal' }` (`src/app/App.tsx:63`, `src/imports/work-hub.html:116`) — but no `bm-role.html` exists in the repo. A foretold internal link with no target — either external/deferred or forgotten.
- **Forgotten — `src/styles/globals.css`.** Empty file (`ls -la` shows 0 bytes). Imported nowhere I could find; `src/main.tsx:4` imports `./styles/index.css` only.
- **Forgotten — `default_shadcn_theme.css`.** 4319 bytes at repo root, no importer.
- **Fulfilled — Pages deploy plumbing.** `.github/workflows/deploy.yml` and `vite.config.ts:20-22` together fulfill the "deploy to GitHub Pages" arc completed in commit `1964b20`.

## 7. Specs

- **No `specs/` directory exists.** Confirmed via `find /home/user/work -type d`.
- **No design documents** in the conventional sense. The closest specs-like artifacts are: (a) the typed config interfaces `Stat`, `ProjectLink`, `Project`, `Task`, `LaunchOption`, `WorkHubConfig` at `src/app/App.tsx:7-44` — these read as a de facto data schema for the hub; (b) the inline CSS variable block at `src/imports/work-hub.html:15-37` — a de facto design-token spec. Categorization is ambiguous; I would *not* file either as a Codex Spec unless Aurelius wants type-definitions-as-specs.

## 8. Companion-log-like artifacts (handoffs, session notes)

- **None found.** No `NOTES.md`, no `HANDOFF.md`, no `sessions/` directory, no companion-attributed commits. All three commits are authored by Rishabh Jain directly, no Claude co-author trailers, no `Generated-by` markers, no session URLs. The branch name `claude/codex-persona-registry-update-t3i7D` is the only Claude signal anywhere in the province — and it points at *no commits beyond `main`*.

## 9. Volume metadata for work-hub

Proposed (and where I would push back on the prior plan):

- **id:** `work-hub`
- **name:** `Work Hub` (the in-product name at `src/imports/work-hub.html:7` and `src/app/App.tsx:156`)
- **shelf:** `active` — it has a live deploy workflow on push to `main` (`.github/workflows/deploy.yml:4-6`) and the most-recent commit is days old in the project timeline. But also defensible as `dormant` since there are only 3 commits and no movement past the initial setup. I would propose `active` and let Aurelius downgrade if the campaign's cadence threshold disagrees.
- **current_phase:** `"v0.1 static prototype — bridge-only Claude integration. Figma Make scaffold codified into React + Vite + Tailwind + shadcn/ui, deployed to GitHub Pages at rishabh1804.github.io/work/. README merge conflict from commit 1964b20 still unresolved on main; deep-link wiring for Claude Code mobile and Desktop deferred pending Anthropic URL schemes."`
- **description (suggested):** `"Personal launcher / landing PWA. Surfaces SEP project status, today's priority items, and one-click 'Launch Claude with context' buttons. Static, no backend, config hardcoded into defaultConfig."`
- **tags (suggested):** `pwa`, `launcher`, `react`, `vite`, `tailwind`, `shadcn`, `figma-make`, `github-pages`
- **repo:** `Rishabh1804/work` (inferred from base path `/work/` at `vite.config.ts:22` and from the deploy-target URL convention).
- **domain_color — counter to the plan's `sand`.** The prior plan proposed `sand`. The Scout counter-proposes a refinement: the hub's actual surface tones are sand / cream (bg `#faf9f5`, surface-2 `#f5f4ed`), but the manifest theme-color and the only saturated brand accent in the province is `#b45a37` — a warm terracotta / clay (`src/imports/work-hub.html:8`, `<meta name="theme-color" content="#b45a37">`). If Codex `domain_color` is meant to be the *identifiable* color, terracotta `#b45a37` is the better signal; if it is meant to be the *dominant surface tone*, sand is correct. Scout's counter: prefer `#b45a37` (terracotta) as the domain_color and keep "sand" as a tag / family descriptor. Flagged as a judgment call.

## 10. Cross-volume references

- **→ soma-internal (Province).** "Codex" button on the SEP project card: `https://github.com/Rishabh1804/soma-internal` (`src/app/App.tsx:60`, `src/imports/work-hub.html:113`).
- **→ sep-dashboard (Province).** "Dashboard" button: `https://rishabh1804.github.io/sep-dashboard/` (`src/app/App.tsx:61`, `src/imports/work-hub.html:114`). Matches existing volume id `sep-dashboard` in `/home/user/Codex/data/volumes.json`.
- **→ sep-invoicing (Province).** "Invoicing" button: `https://github.com/Rishabh1804/sep-invoicing` (`src/app/App.tsx:62`, `src/imports/work-hub.html:115`). Matches existing volume id `sep-invoicing`.
- **→ Anthropic / Claude.ai (external).** `launchClaude()` opens `https://claude.ai/new?q=...` with pre-baked context strings (`src/app/components/LaunchPanel.tsx:19, 27`; `src/imports/work-hub.html:188, 199`). Notable: the context strings reference *paths inside another province* — `"Read codex/operations/snapshot.md and codex/tasks.md to bootstrap SEP context."` (`src/app/components/LaunchPanel.tsx:11-12`, `src/imports/work-hub.html:181-184`). Those paths almost certainly live in the soma-internal Province.
- **→ AI projects (foretold Province).** Placeholder card (`src/app/App.tsx:66-72`).
- **No prior cross-references inbound.** Greps of `/home/user/Codex/data/*.json` for `work-hub`, `workhub`, `work_hub` — zero hits. Likewise zero hits for `soma-internal` across existing Codex data. This survey is genuinely the first archival mention for both.

## 11. Data-integrity surprises

- **README merge conflict, committed to main and never resolved.** `README.md:1-15` contains raw `<<<<<<< HEAD ... ======= ... >>>>>>> 03b023465efeea5b0bbd5504f6580d6c6f5ae2bb` markers. The conflicting branches were `50775e4` (Figma Make "Review HTML Code" README) vs `03b0234` (single-line "# work" README). The merge commit `1964b20` recorded the conflict as the file content rather than resolving it — both `main` and `claude/codex-persona-registry-update-t3i7D` point at the same SHA, so this is shipped on the default branch and on the deploy source. Severity: low for users (README isn't served by Pages), medium for hygiene.
- **Title drift.** `index.html:7` still says "Review HTML Code" — the Figma Make default — even though the app and the manifest both say "Work Hub". A user landing on the deployed site sees "Review HTML Code" in their browser tab.
- **Package name drift.** `package.json:2` is `"@figma/my-make-file"` — Figma Make default, never renamed.
- **Empty `src/styles/globals.css`** (0 bytes) and **orphaned `default_shadcn_theme.css`** (no importer) — see §6.
- **Manifest referenced but absent.** `src/imports/work-hub.html:9` references `manifest.json`; `src/imports/work-hub.html:10` references `icons/icon-192.png`. Neither file exists in the repo. The HTML import is essentially a snapshot from a different deployment context and won't render with those PWA features if served standalone.
- **Internal link to nonexistent page.** `bm-role.html` (`src/app/App.tsx:63`, `src/imports/work-hub.html:116`) — see §6.
- **Two near-identical UIs in the repo.** The HTML import in `src/imports/` and the React app in `src/app/` are functional duplicates. They will drift unless one is consciously authoritative. Currently `src/main.tsx` mounts the React one.
- **Hardcoded user name in two places.** `'Rishabh'` at `src/app/App.tsx:48` and `src/imports/work-hub.html:89, 172-174`. Personalization is baked in, not configured.

## 12. Counts — approximate ingest estimate

Best read of what would actually populate Codex's per-Volume entity tables for this Province:

| Entity | Count | Notes |
|---|---|---|
| volume (this Province) | 1 | the founding entry itself |
| chapters | ~3 | one per commit, or compressed into 2 (`scaffold-codified`, `pages-deploy-and-merge`); ambiguous |
| todos | 2 | both Claude deep-link TODOs (mobile + desktop). Mirrored across `.tsx` and `.html` — count once per concept. |
| canons | ~4 | the two `vite.config.ts` invariants, the postcss note, the deploy base-path coupling |
| schisms | 0–1 | HTML-monolith → React port, if Aurelius wants it captured (inferred, not declared) |
| lore | ~2–3 | Origins (3-commit founding), Cautionary Tale (the shipped merge conflict), and optionally the "two design systems" thread |
| apocrypha | ~6 | 2 foretold (deep-links), 1 foretold-empty (AI projects), 1 foretold-missing (`bm-role.html`), 2 forgotten (`globals.css` empty, `default_shadcn_theme.css` orphan); plus 1 fulfilled (Pages deploy) if fulfilled is tracked |
| specs | 0 | none formally; see §7 caveat about type-defs |
| companion-logs | 0 | none found |
| doctrine-ledger entries | 0 | none found |
| cross-volume references | 3 confirmed + 1 foretold | → soma-internal, sep-dashboard, sep-invoicing; → "AI projects" |

**Estimated ingest weight:** small. ~18–22 entries total across all entity types, dominated by apocrypha and canons. This Province is light on textual artifacts and heavy on cross-references and TODOs.

---

## Open rulings for the Chronicler

Deferred to the Tier 0 PR #2 schema doc, or to the per-Province ingestion PR in Tier 4. Recorded here so they are not lost.

1. **Type-definitions-as-specs question (§7)** — whether the `WorkHubConfig` and related interfaces in `src/app/App.tsx:7-44` constitute Codex Specs. Scout would not file unless Aurelius rules yes.
2. **SEP-task attribution (§2, §12)** — whether the five SEP tasks displayed in the hub should be ingested as `todos[]` against work-hub or solely against soma-internal as cross-province surface area. Scout's instinct: attribute to soma-internal; keep work-hub's archival profile thin (work-hub's nature is to be a launcher, not an owner). Cross-validate against the soma-internal Scout's findings before ruling.
3. **domain_color (§9)** — Scout counter-proposes `#b45a37` (terracotta) over the prior plan's `sand`. Decision rule needed: is `domain_color` the *identifiable* color or the *dominant surface tone*?
4. **README merge-conflict (§11)** — separate from archival ingest; flagged as a hygiene fix work-hub itself should resolve. Belongs in Tier 5 (cross-cutting cleanups).

---

*Survey conducted 2026-05-24 by Scribe-Scout under summon by Aurelius. Drafted into archival form by Aurelius from the Scout's findings brief. Permission floor of canon-proc-006 observed: Scout performed no Writes, no Edits, no commits.*
